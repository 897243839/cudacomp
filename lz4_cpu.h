
// Benchmark performance from the binary data file fname

// 定义 CChar 类
#define CChar unsigned char
int  call_run_example();
int test();
void print_string(char *s);
int  run_example(char *s);
int go_data(char* data, int len);
char* go_data_re(char* data, int len,size_t* size);
char* go_data_comp(char* data, int len,size_t* size);
